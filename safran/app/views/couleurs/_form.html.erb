<%= form_for(@couleur) do |f| %>
  <% if @couleur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@couleur.errors.count, "error") %> prohibited this couleur from being saved:</h2>

      <ul>
      <% @couleur.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
